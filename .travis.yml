# Config file for automatic testing at travis-ci.org

sudo: required

language: python

python: 3.6

script:
    - pytest

install:
    - pip install -r requirements.txt

notifications:
  email:
    on_success: change
    on_failure: always